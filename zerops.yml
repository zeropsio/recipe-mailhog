mailhog:
  build:
    base:
      - go@1
    build:
      - go install github.com/mailhog/MailHog@latest
      - mv ~/go/bin/MailHog /build/source/mailhog
    deploy:
      - mailhog
  run:
    start: ./mailhog
